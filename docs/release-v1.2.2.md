# Ver1.2.2 Display Hotfix

- `online / docker-container-running / docker-container-running` の重複表示を整理
- `docker-container-running` を `Docker稼働` として表示
- `fast-tcp` を `TCP疎通` として表示
- Tailscaleの `assumed_online` を `利用中` 表示へ変更
- `manual-server` を意図的停止として警告除外
- Docker除外済みセクションを追加
